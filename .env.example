# Backend API URL
# All AI calls are proxied through this server (AWS credentials live server-side)
VITE_API_URL=http://localhost:8000

# Demo mode — set to "true" to run the app with realistic mock data (no backend needed)
VITE_DEMO_MODE=false
